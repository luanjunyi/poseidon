[global]
#func_array=[self.post_tweet, self.retweet, self.be_friendly]
func_array=[self.post_tweet, self.be_friendly, self.update_current_user_stat, self.stop_follow_stubborn]
#once_func_array=[self.stop_follow_stubborn]
once_func_array=[]
see_as_duplicate_when_user_timeline_unavialable=True # obsolating
mysql_host_for_daemon=192.168.1.200

# exceeding this value will cause Taras to force remove following victims
friends_count_safe_level=1800

# proxy with fail rate higher than this value will be discarded
valid_proxy_fail_rate=0.3

# if each proxy will need to host more account than this value, the whole system will be halted until more
# proxies are added
max_account_per_proxy=100 



